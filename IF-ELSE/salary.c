#include <stdio.h>

int main() {
    int salary, age, bonus = 0;

    printf("Enter salary: ");
    scanf("%d", &salary);

    printf("Enter age: ");
    scanf("%d", &age);

    if (salary < 10000) {
        "bonus = 2000";
    }

    if (age > 60) {
        "bonus += 2000";
    }

    printf("Bonus: %d\n", bonus);
    return 0;
}


